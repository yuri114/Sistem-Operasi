================================================================
  Oria OS  ESTATUS IMPLEMENTASI
  Terakhir diperbarui: 6 Mei 2026
  Platform: x86-64 Long Mode | QEMU qemu-system-x86_64
  Build: WSL x86_64-linux-gnu-gcc | .\build.ps1 build
  Kernel: ~569 KB | Resolusi: 1920x1080 32bpp VBE
================================================================


================================================================
  ARSITEKTUR INTI
================================================================

BOOT & CPU
  [x] Real Mode -> Protected Mode -> 64-bit Long Mode (IA-32e)
  [x] GDT64: kernel CS/DS, user CS/DS, TSS64 per-CPU
  [x] TSS64 per-core (RSP0 diupdate tiap task switch)
  [x] IDT 64-bit: 15 GPR, exc 0-14, IRQ 0-15
  [x] SYSCALL/SYSRET (bukan int 0x80)
  [x] LAPIC enable per-CPU
  [x] SMP bootstrap: ACPI MADT -> INIT/SIPI -> AP online (QEMU -smp 2)
  [x] Per-AP LAPIC timer (INT 0x40, 10ms, dikalibrasi dari BSP)
  [x] CPU feature detection via CPUID
  [x] x87/SSE state save/restore saat context switch (FXSAVE/FXRSTOR di task_switch)
  [ ] NMI handler
  [ ] ACPI power management (S3/S4 suspend)
  [ ] UEFI boot (saat ini BIOS/legacy only via GRUB)
  [ ] CPU affinity: SYS_SCHED_SETAFFINITY (pin task ke core tertentu)

MEMORI
  [x] 4-level paging (PML4/PDPT/PD/PT), identity-map 4GB
  [x] PMM bitmap 64MB (16384 frames x 4KB)
  [x] vmm_map_page / vmm_unmap_page + invlpg
  [x] vmm_create_page_dir / vmm_free_user_memory (COW-aware)
  [x] Demand paging: #PF handler map frame on-demand (user space)
  [x] Guard page stack user: 0x5FE000 (STACK OVERFLOW detection)
  [x] Guard page thread: 0x700000+id*0x5000 (THREAD STACK OVERFLOW)
  [x] User heap via SYS_BRK (0x400000..0x5FDFFF)
  [x] Kernel heap: kmalloc/kfree (0x100000..0x6FFFFF)
  [x] mmap/munmap halaman anonim (SYS_MMAP/MUNMAP)
  [x] mmap_file / munmap_file (SYS_MMAP_FILE/MUNMAP_FILE)
  [ ] Swap / page eviction ke disk
  [ ] Large page (2MB) support untuk user space
  [ ] KASLR (kernel address space layout randomization)

MULTITASKING
  [x] Round-robin scheduler, MAX_TASKS=32
  [x] Per-core scheduler: BSP (user+float tasks), AP (kernel tasks)
  [x] Load-balanced AP dispatch: assigned task langsung ke AP
  [x] task_create / task_create_user / task_create_thread
  [x] task_exit: bebaskan page_dir, fd, thread stack
  [x] task_block / task_unblock (blocking sejati, bukan sleep loop)
  [x] task_wait / waitpid (foreground exec block sampai child selesai)
  [x] fork() + COW (Copy-on-Write), exec_replace()
  [x] Thread: berbagi page_dir+heap, stack 16KB + guard per-thread
  [x] Thread naming (SYS_THREAD_SET_NAME)
  [x] Thread-Local Storage (TLS) via FS MSR (0x800000+tid*0x1000)
  [x] Signal: SIGKILL(9), SIGTERM(15), SIGINT(2)  Epending_signals bitmask
  [x] Signal handler user (SYS_SIGACTION)
  [x] Ctrl+C -> SIGINT ke foreground process
  [x] exit code: task_exit(code), SYS_WAITPID kembalikan code
  [x] Nice value per-task (task_set_nice)
  [x] Zombie cleanup ? slot langsung dibebaskan (used=0), tidak ada zombie state
  [ ] setuid / setgid
  [ ] Proses group / session (setsid, tcsetpgrp)
  [x] Job control: fg/bg/jobs (tabel job, jobs_add/remove, fg %N wait, bg %N)
  [x] POSIX signal mask (sigprocmask, sigpending)
  [ ] Real-time priority class
  [x] ulimit / rlimit per proses (CPU time, memori, fd count)
  [ ] CPU affinity per-task (sched_setaffinity)


================================================================
  DRIVER & HARDWARE
================================================================

DISPLAY
  [x] VBE linear framebuffer 1920x1080 32bpp
  [x] draw_pixel, fill_rect, draw_line, draw_char, draw_str
  [x] Font 8x8 bitmap (font8x8.h)
  [x] Scrolling teks kernel
  [x] ANSI/VT100 escape codes di print_char (SGR + cursor control)
  [x] Framebuffer double-buffering (flicker-free) ? back_buf + gfx_flip()
  [ ] Hardware cursor
  [ ] Resolusi dinamis (user bisa ganti resolusi)
  [ ] Truetype / vector font rendering

STORAGE
  [x] ATA PIO driver: read/write sector, detect drive
  [x] ATA Bus Master DMA (bmide_detect, dma_transfer, PIO fallback)
  [x] VirtIO Block device (virtio_blk.c): MMIO, virtqueue
  [x] Ext2 filesystem read-only: inode, readdir, readfile
  [ ] Ext2 write support
  [ ] AHCI SATA driver
  [ ] NVMe driver
  [ ] GPT partition table parser (hanya MBR saat ini)

INPUT
  [x] PS/2 keyboard driver: scancode set 1, shift/ctrl/alt/caps/F1-F10
  [x] PS/2 mouse driver: klik kiri/kanan, scroll
  [x] Keyboard blocking sejati (keyboard_set_waiter / task_unblock)
  [x] Driver keyboard VGA (drv_kbd.c) + driver VGA output (drv_vga.c)
  [ ] USB HID keyboard/mouse
  [ ] Gamepad / joystick

SERIAL & DEBUG
  [x] COM1 serial driver 9600 baud 8N1
  [x] Kernel debugger interaktif via serial (kdbg)
  [x] dmesg ring buffer 4096 byte (dmesg_log / dmesg_read)
  [ ] Serial console sebagai tty penuh (login via serial)
  [ ] Remote GDB stub

TIMER & WAKTU
  [x] PIT 1000 Hz (1 tick = 1ms), get_ticks(), msleep_kernel()
  [x] LAPIC timer per-core (10ms, kalibrasi dari BSP PIT)
  [x] RTC driver CMOS: baca detik/menit/jam/tgl/bulan/tahun
  [x] NTP date sync via UDP (ntpdate command)
  [ ] HPET driver
  [x] Alarm / setitimer (SIGALRM) ? task_set_itimer() + SYS_SETITIMER(104)
  [ ] Timezone support

PCI & BUS
  [x] PCI config space scan Type 1 (32-bit I/O port): vendor/device ID, BAR
  [x] PCI used by RTL8139 (NIC), ATA DMA (Bus Master IDE), VirtIO Block
  [ ] Generic PCI driver registration framework
  [ ] PCI-e MSI/MSI-X interrupt routing (saat ini hanya legacy IRQ)
  [ ] USB OHCI/EHCI/xHCI host controller stack

NETWORKING
  [x] RTL8139 NIC driver (polling, PCI scan, TX/RX ring)
  [x] Ethernet II frame build/parse
  [x] ARP request/reply + ARP cache (8 slot)
  [x] IPv4: header, checksum, routing (direct / gateway 10.0.2.2)
  [x] ICMP echo request/reply (ping)
  [x] UDP send/receive
  [x] TCP stack: connect, send, recv, close (SYN/SYNACK/FIN/RST)
  [x] TCP retransmit timer + out-of-order buffer
  [x] TCP keepalive + idle timeout
  [x] DNS resolver UDP (A record, server 8.8.8.8)
  [x] HTTP/1.0 client curl: GET, chunked decode, redirect, status warna
  [x] HTTPS/TLS 1.3: X25519, AES-128-GCM, SHA-256, HKDF
  [x] X.509 sertifikat parse (tanpa verifikasi chain)
  [x] HTTP server (httpd): serve file dari MFS4
  [x] WebSocket server: upgrade + frame parser
  [x] DHCP client (saat ini hardcode 10.0.2.15)
  [ ] IPv6
  [ ] UDP socket bind/recv dari user space
  [x] TCP listen / accept (server socket dari user space) ? SYS_NET_LISTEN/ACCEPT/UNLISTEN (101-103)
  [ ] TLS verifikasi sertifikat (trust store)
  [ ] VirtIO Network driver (virtio-net)
  [ ] TCP window scaling / Nagle algorithm
  [ ] Raw socket (kirim frame IP/Ethernet langsung dari user space)
  [ ] Unix domain socket (AF_UNIX, komunikasi antar proses lokal)


================================================================
  FILESYSTEM
================================================================

MFS4 (filesystem utama in-memory + disk persistence)
  [x] FS_MAX_FILES=64, FS_MAX_DATA=65536 byte per file
  [x] Inode layer: symlink, hardlink, stat (type/size/ctime/mtime/perms)
  [x] fs_mkdir / fs_list_dir / fs_get_perms / fs_set_perms
  [x] fsync / dirty bit -> flush ke disk ATA
  [x] tmpfs flag (file tidak di-persist ke disk)
  [x] Disk persistence: inode table di sektor 514+ (mfs4_flush/mfs4_load)
  [x] Rename file (mfs4_rename)
  [x] chmod <file> <hex_perm>
  [x] Max file size > 65536 byte ? FS_MAX_BIN_DATA=512KB, malloc exact size
  [ ] File locking (flock)
  [ ] Direktori bersarang > 1 level

VFS (Virtual Filesystem)
  [x] VFS fd table per-task (MAX_TASKS x VFS_MAX_FD=8)
  [x] Tipe fd: STDIN, STDOUT, FILE, PIPE, NET, TTY, PROC
  [x] vfs_open/read/write/close/seek
  [x] fd diwarisi fork (vfs_copy_fds)
  [x] vfs_redirect_out / vfs_redirect_in / vfs_redirect_out_append
  [x] Non-blocking fd (VFS_O_NONBLOCK, return EAGAIN)
  [x] poll() syscall (POLLIN/POLLOUT/POLLERR, timeout)
  [x] /proc virtual: ps, meminfo, uptime, cpuinfo (real data), dmesg
  [ ] /dev virtual filesystem
  [x] /proc/<pid>/status  EName, Pid, State, PPid, Priority
  [x] /proc/<pid>/maps, /proc/<pid>/fd ? proc_generate() extended
  [ ] inotify / file watch API
  [x] VFS_MAX_FD > 8 ? sudah 32 (vfs.h line 12)

Ext2 (disk kedua disk.img)
  [x] Mount ext2 read-only: mount ext2
  [x] ext2ls [path]: list direktori
  [x] ext2cat <file>: baca isi file
  [ ] Ext2 write support
  [ ] Mount ext2 sebagai root filesystem


================================================================
  IPC & SINKRONISASI
================================================================

  [x] Semaphore: alloc/free/wait/post, blocking sejati, FIFO ring 4 waiter
  [x] Condition variable: alloc/free/wait/signal/broadcast
  [x] Futex: FUTEX_WAIT/WAKE (CAS + block/wake atomik)
  [x] mutex_lock/unlock di lib.h via futex
  [x] Pipe anonim: alloc/read/write/close, blocking sejati, EOF propagasi
  [x] Named pipe: open/close by-name
  [x] Shared memory: 8 region x 4KB (VA 0x500000+slot*4096)
  [x] Message queue per-task: mq_send/mq_recv (8 pesan x 56 byte)
  [x] Clipboard: SYS_CLIP_COPY/PASTE (512 byte shared buffer)
  [ ] SysV semaphore (semget/semop)
  [ ] POSIX message queue (mq_open/mq_send/mq_receive)
  [ ] eventfd / signalfd
  [ ] Unix domain socket (AF_UNIX socketpair)


================================================================
  SHELL & SCRIPTING
================================================================

Built-in commands
  [x] help, clear, about, reboot, shutdown/poweroff
  [x] echo [teks / $VAR / $? / $$], date, time, uptime
  [x] ls [path] [-l long format], cd [path], pwd
  [x] mkdir <nama>, chmod <nama> <hex>, sync
  [x] read <VARNAME>    Ebaca baris keyboard ke variabel
  [x] read <file>       Etampilkan isi file
  [x] write <file> <teks>
  [x] del <file>, rename <lama> <baru>
  [x] cat [file...], cp <src> <dst>, mv <src> <dst>
  [x] wc [file], head [-n N] [file], tail [-n N] [file], less/more [file]  Epager
  [x] find <dir> <pola>, stat <file>, touch <file>
  [x] df          Eslot file MFS4 yang dipakai/bebas
  [x] du [path]   Etotal ukuran file di path
  [x] sort [file] / stdin  Eurutkan baris teks
  [x] dmesg       Etampilkan kernel ring buffer log
  [x] ps, kill [-sig] <pid>, nice <n> <cmd>, renice <pid> <n>
  [x] env, export KEY=VAL
  [x] meminfo, cpuinfo, taskstat, paging
  [x] history     Eriwayat 50 perintah
  [x] open/fread/fwrite/fclose  Efd manual
  [x] mq_send <pid> <pesan>, mq_recv
  [x] ping <ip>, ifconfig, udp_send, tcp_get, curl <url>
  [x] nslookup <host>, ntpdate, certcheck [host]
  [x] vdisk, mount ext2, ext2ls, ext2cat
  [x] httpd [start [port] | stop]
  [x] ws <url>    EWebSocket client
  [x] kdbg        Ekernel debugger via serial
  [x] whoami, login, adduser, passwd, su
  [x] edit <file>  Etext editor nano-like
  [x] sh [script.sh]  Ejalankan script shell
  [x] jobs, fg %N, bg %N  Ejob control
  [x] which <cmd>
  [x] alias / unalias
  [x] top          Erealtime process monitor (refresh 1s, keluar tombol apa pun)
  [x] set -e / set -x untuk debug script

Shell features
  [x] Pipeline multi-stage: prog1 | prog2 | ... | prog6
  [x] Redirect output: prog > file
  [x] Redirect append: prog >> file
  [x] Redirect input: prog < file
  [x] Background: cmd &
  [x] Conditional: cmd1 && cmd2, cmd1 || cmd2
  [x] Variable expansion: $VAR, $?, $$
  [x] Auto-exec (program tanpa prefix 'exec')
  [x] Tab completion (command + filename)
  [x] Command history (arrow up/down)
  [x] ANSI/VT100 output dari program user
  [x] Script: if/else/fi, while/do/done, for/in/do/done
  [x] Script: $1..$9 argumen, komentar #
  [x] Here-doc (<<EOF) ? shell_heredoc_detect() + interactive collection mode
  [x] Glob pattern *, ? dalam argument expansion
  [x] Subshell $(...) command substitution ? shell_expand_subshell()
  [x] Brace expansion {a,b,c} ? shell_expand_brace()
  [x] Arithmetic expansion $(( expr )) dan let
  [x] Fungsi shell (function name { ... }) dalam script
  [ ] trap builtin (tangkap sinyal dalam script, e.g. trap handler INT)


================================================================
  USER SPACE & LIBRARY (lib.h)
================================================================

  [x] Syscall wrappers syscall0..3 via SYSCALL instruction
  [x] print / exit / getkey / sleep / yield / getpid
  [x] malloc / free: user heap via SYS_BRK, linked list + coalescing
  [x] malloc thread-safe via futex mutex
  [x] memcpy, memset, memmove, memcmp
  [x] strlen, strcpy, strcat, strcmp, strstr, strncpy
  [x] sprintf / vsprintf / printf: %d %i %u %x %s %c %p %%
  [x] atoi / strtol (base 0/8/10/16)
  [x] itoa (basis 10)
  [x] getargv / argc/argv dari stack (System V AMD64 ABI)
  [x] fork / exec / exec_replace / waitpid
  [x] thread_create / thread_exit / thread_join / thread_set_name
  [x] sem_alloc/free/wait/post
  [x] cond_alloc/free/wait/signal/broadcast
  [x] mutex_lock/unlock via futex
  [x] mmap / munmap
  [x] sys_open / sys_read_fd / sys_write_fd / sys_close_fd
  [x] fcntl_setfl / poll (POLLIN/POLLOUT, timeout)
  [x] signal / kill
  [x] dns_resolve (UDP, A record)
  [x] net_tcp_connect/send/recv/close
  [x] clipboard_copy / clipboard_paste
  [x] tls_get (FS MSR TLS base)
  [x] scanf / sscanf (format: %d %u %x %s %c)
  [x] qsort / bsearch
  [x] time() / gettimeofday() ? SYS_TIME(106), get_ticks()/1000
  [x] errno global per-thread ? via TLS offset 8, TLS dialokasi untuk semua user task
  [x] FILE* stdio (fopen/fread/fwrite/fclose/fseek) + fprintf/fputs/fgets/fgetc/fputc + lseek
  [x] Floating point support (printf %f) ? vsprintf %f/F handler, 6 decimal
  [ ] Dynamic library loading (.so)

User programs (ELF64, di-embed di kernel image)
  [x] hello, sender, writer           Edemo dasar
  [x] piper, pipe_sender, pipe_receiver  Edemo pipe
  [x] devtest                         Edemo device driver
  [x] gfxtest                         Edemo framebuffer
  [x] gui_demo, gui_term              Edemo window manager
  [x] paint, calc, notepad, filemanager, clock, sysinfo  EGUI apps
  [x] threadtest, condtest, forktest  Edemo threading/sync
  [x] pipetest, polltest, futextest   Edemo IPC/poll/futex
  [x] mfs4test, sigtest               Edemo MFS4 + signal
  [x] grep, sort, tail, cat, wc, head, cp, mv, ls, edit  Etools Unix
  [ ] awk, sed, less/more, zip/gzip
  [ ] xxd / hexdump
  [ ] tar (archiver)
  [ ] bc (calculator CLI, dependensi arithmetic expansion)


================================================================
  AUDIO
================================================================

  [ ] PC Speaker driver: beep / nada via port 0x61 + PIT channel 2
  [ ] AC'97 audio codec driver (PCI, QEMU -soundhw ac97)
  [ ] Intel HDA (High Definition Audio) driver
  [ ] /dev/audio: device node untuk output PCM
  [ ] Software mixing: campurkan beberapa stream audio
  [ ] SYS_AUDIO_WRITE syscall (kirim buffer PCM ke /dev/audio)

================================================================
  GUI & WINDOW MANAGER
================================================================

  [x] Window manager: max 16 window, drag, minimize, close
  [x] Taskbar: daftar window aktif, klik untuk focus/restore
  [x] Wallpaper: load raw BGRA32 960x540 dari disk (wallpaper.img)
  [x] Window syscalls: create/destroy/draw/clear/event/btn_add
  [x] Mouse: klik, drag window, scroll
  [x] Clipboard copy/paste antar window
  [ ] Window resize oleh user (drag border)
  [ ] Compositing / alpha blending
  [ ] Ikon desktop / shortcut
  [ ] Notifikasi / popup toast
  [ ] Font TrueType / anti-aliased
  [ ] Drag-and-drop antar window
  [ ] Multi-monitor / virtual desktop support


================================================================
  DEBUGGING & DIAGNOSTICS
================================================================

  [x] kdbg: kernel debugger interaktif via serial (breakpoint, reg dump)
  [x] dmesg ring buffer 4096 byte
  [x] Kernel oops / page fault dump (register + CR2 ditampilkan)
  [ ] Kernel panic screen dengan full stack trace
  [ ] Backtrace / stack unwinder kernel (RBP chain walk)
  [ ] /proc/kallsyms: symbol table kernel (addr → nama fungsi)
  [ ] Core dump user process ke file saat segfault
  [ ] Flame graph / profiler via LAPIC timer sampling

================================================================
  KEAMANAN & USER MANAGEMENT
================================================================

  [x] Ring-3 isolasi penuh (user space tidak bisa akses kernel)
  [x] is_user_ptr() validasi pointer syscall
  [x] User accounts: /etc/passwd, /etc/shadow (hash SHA-256)
  [x] login / whoami / su / passwd / adduser
  [x] chmod permission bits per file (MFS4 perms)
  [ ] Mandatory access control (MAC)
  [ ] Sandbox / seccomp filter
  [ ] Stack canary (compile-time)
  [ ] ASLR untuk user space
  [ ] Audit log syscall
  [ ] SMEP/SMAP enforcement (blokir kernel baca/eksekusi user page)
  [ ] Kernel exploit mitigations: KPTI (Meltdown isolation)


================================================================
  SYSCALL INDEX (107 total, SYS 0-106)
================================================================

  SYS 0   print              SYS 50  fs_tmpwrite
  SYS 1   getkey             SYS 51  fs_mkdir
  SYS 2   exit               SYS 52  pipe_named
  SYS 3   alloc              SYS 53  shm_create
  SYS 4   free               SYS 54  shm_attach
  SYS 5   fs_read            SYS 55  shm_detach
  SYS 6   fs_write           SYS 56  open
  SYS 7   msg_send           SYS 57  read_fd
  SYS 8   msg_recv           SYS 58  write_fd
  SYS 9   kill               SYS 59  close_fd
  SYS 10  sem_alloc          SYS 60  mq_send
  SYS 11  sem_free           SYS 61  mq_recv
  SYS 12  sem_wait           SYS 62  brk
  SYS 13  sem_post           SYS 63  waitpid
  SYS 14  pipe_open          SYS 64  thread_create
  SYS 15  pipe_write         SYS 65  thread_exit
  SYS 16  pipe_read          SYS 66  thread_join
  SYS 17  pipe_close         SYS 67  thread_set_name
  SYS 18  pipe_getid         SYS 68  cond_alloc
  SYS 19  dev_write          SYS 69  cond_free
  SYS 20  dev_read           SYS 70  cond_wait
  SYS 21  dev_ioctl          SYS 71  cond_signal
  SYS 22  draw_pixel         SYS 72  cond_broadcast
  SYS 23  fill_screen        SYS 73  fork
  SYS 24  fill_rect          SYS 74  exec_replace
  SYS 25  draw_line          SYS 75  mmap
  SYS 26  clr_screen         SYS 76  munmap
  SYS 27  getpid             SYS 77  pipe2
  SYS 28  yield              SYS 78  net_open
  SYS 29  sleep              SYS 79  tty_open
  SYS 30  exec               SYS 80  pipe_redirect
  SYS 31  draw_char          SYS 81  mfs4_symlink
  SYS 32  draw_str           SYS 82  mfs4_hardlink
  SYS 33  mouse_get          SYS 83  mfs4_stat
  SYS 34  win_create         SYS 84  mfs4_listdir
  SYS 35  win_destroy        SYS 85  mfs4_unlink
  SYS 36  win_draw           SYS 86  mfs4_mkdir
  SYS 37  win_clear          SYS 87  sigaction
  SYS 38  win_event          SYS 88  sigkill_sig
  SYS 39  win_btn_add        SYS 89  futex_wait
  SYS 40  win_click_pos      SYS 90  futex_wake
  SYS 41  win_draw_pixel     SYS 91  get_tls
  SYS 42  win_fill_rect      SYS 92  mfs4_rename
  SYS 43  win_mouse_rel      SYS 93  fcntl
  SYS 44  win_minimize       SYS 94  poll
  SYS 45  win_restore        SYS 95  getargv
  SYS 46  fs_list            SYS 96  clip_copy
  SYS 47  fs_delete          SYS 97  clip_paste
  SYS 48  get_ticks          SYS 98  mmap_file
  SYS 49  fs_sync            SYS 99  munmap_file

  SYS 100  lseek             SYS 104  setitimer
  SYS 101  net_listen        SYS 105  gfx_flip
  SYS 102  net_accept        SYS 106  time
  SYS 103  net_unlisten
